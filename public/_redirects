# API proxy — Netlify forwards /api/* requests to the Vercel backend (server-side, no CORS)
/api/*  https://theo-sign-h7wb.vercel.app/api/:splat  200
# SPA fallback — all other routes serve index.html
/*    /index.html   200
